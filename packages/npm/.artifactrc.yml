upsert:
  .github/**/*.yml:
    if_exists: overwrite

update:
  CHANGELOG.md:
    if_exists: skip

  package.json:
    transforms:
      - description: Remove release-it from overrides
        jq: 'del(.overrides["release-it"])'
