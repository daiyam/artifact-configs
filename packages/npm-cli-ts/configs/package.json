{
	"engines": {
		"node": ">=18.20"
	},
	"type": "module",
	"bin": {
		"#[[vars.BINARY_NAME]]": "bin/#[[vars.BINARY_NAME]]"
	},
	"scripts": {
		"build": "npm run clean && npm run compile:src",
		"prepack": "npm run build",
		"watch:build": "tsc-watch -p src --onSuccess 'npm run build'"
	},
	"dependencies": {
		"@zokugun/cli-utils": "0.2.1",
		"execa": "9.6.1",
		"trash": "10.1.1"
	}
}
