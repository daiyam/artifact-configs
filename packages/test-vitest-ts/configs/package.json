{
	"scripts": {
		"clean": "rimraf .test",
		"compile:test": "tsc -p test",
		"test": "vitest run",
		"test:watch": "vitest",
		"watch:test": "tsc-watch -p test"
	},
	"devDependencies": {
		"@types/fs-extra": "^11.0.4",
		"fs-extra": "^11.3.3",
		"globby": "^16.1.0",
		"vitest": "^4.0.16"
	}
}
