{
	"main": "lib/index.js",
	"scripts": {
		"compile": "tsc -p src",
		"prepublishOnly": "npm run compile",
		"release": "release-it",
		"watch": "tsc -watch -p src"
	},
	"devDependencies": {
		"release-it": "^18.1.2"
	}
}
